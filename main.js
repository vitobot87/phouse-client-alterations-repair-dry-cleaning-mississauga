const root=document.documentElement,btn=document.getElementById('theme'),mq=matchMedia('(prefers-color-scheme: dark)'),states=['system','light','dark'],icons={system:'◐',light:'☼',dark:'☾'};function resolved(t){return t==='system'?(mq.matches?'dark':'light'):t}function apply(t){root.dataset.theme=t;root.classList.toggle('dark',resolved(t)==='dark');btn.textContent=icons[t];btn.title=`Theme: ${t}`;btn.setAttribute('aria-label',`Theme: ${t}`);localStorage.setItem('theme',t)}apply(localStorage.getItem('theme')||'system');btn.onclick=()=>apply(states[(states.indexOf(root.dataset.theme)+1)%states.length]);mq.addEventListener?.('change',()=>{if(root.dataset.theme==='system')apply('system')});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>document.getElementById('menu')?.classList.add('hidden')));
